- if @document.valid?
  .alert.alert-success
    %button.close{data: {dismiss: 'alert'}} &times;
    Document 
    %strong= @document.file.filename
    was successfully uploaded.
- else
  .alert.alert-error
    %button.close{data: {dismiss: 'alert'}} &times;
    Document 
    %strong= @document.file.filename
    wasn't uploaded.

